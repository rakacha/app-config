logging:
    level:
        io:
            pivotal: DEBUG

customer:
  queue: spring-boot-customer

item:
  service:
    name: item-236220

ribbon:
  IsSecure: false

management:
    endpoints:
        web:
            exposure:
                include: "*"
                
server:
  port: 8082
